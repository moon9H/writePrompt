<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.ssafy.wp.model.dao.MemberDao">
	
	<select id="select" resultType="Member">
		SELECT id, email, password, role, nickname,
				age, gender, profile, created_time
		FROM user
		<where>
			<if test="id != null and id != ''">
				AND id = #{id}
			</if>
		</where>
	</select>
	
	<insert id="insert" parameterType="Member"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user
			(email , password, role, nickname,
			age, gender, profile)
		VALUES
			(#{email}, #{password}, #{role}, #{nickname},
			#{age}, #{gender}, #{profile})
	</insert>
	
	<update id="update" parameterType="Member">
		UPDATE user
		<set>
			<if test="nickname != null and nickname != ''">
				nickname = #{nickname},
			</if>
			<if test="age != null">
				age = #{age},
			</if>
			<if test="gender != null and gender != ''">
				gender = #{gender},
			</if>
			<if test="profile != null and profile != ''">
				profile = #{profile},
			</if>
		</set>
		WHERE id = #{id}
	</update>
	
	<delete id="delete">
		DELETE 
			FROM user
		WHERE 
			id = #{id}
	</delete>
	
	<select id="selectByEmail" resultType="Member">
		SELECT id, email, password, role, nickname,
				age, gender, profile, created_time
		FROM user
		<where>
			<if test="id != null and id != ''">
				AND email = #{email}
			</if>
		</where>
	</select>
</mapper>